#include <stdio.h>

/*
First parameter: argc, for argument count
Second parameter: argv, for argument vector

argv is a pointer to an array of character strings that contain the arguments,
one per string. More on this later.
*/
int main(int argc, char *argv[]) {
  printf("Hi 👋🏽 L👀k, main has parameters!\n");

  return 0;
}
